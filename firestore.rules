rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Per-user dictionary blob: users/{uid}/sync/dictionary
    match /users/{userId}/sync/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
